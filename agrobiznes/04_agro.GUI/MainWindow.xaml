<Window x:Class="_04_agro.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_04_agro.GUI"
        mc:Ignorable="d"
        Title="Agrobiznes" Height="495" Width="978">

    <Grid Background="#1E1E1E" Margin="10" TextElement.Foreground="White">

        <!-- ====== RESOURCES ====== -->
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="13"/>
            </Style>

            <Style TargetType="Label">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="13"/>
            </Style>

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
            </Style>

            <Style x:Key="CardBorderStyle" TargetType="Border">
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Background" Value="#2D2D30"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="Margin" Value="6"/>
            </Style>

            <Style TargetType="Button">
                <Setter Property="Height" Value="34"/>
                <Setter Property="Margin" Value="0,6,0,0"/>
                <Setter Property="Background" Value="#3E3E42"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#5A5A5A"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <Style TargetType="ProgressBar">
                <Setter Property="Height" Value="18"/>
                <Setter Property="Margin" Value="0,4,0,0"/>
            </Style>

            <Style TargetType="ListBox">
                <Setter Property="Background" Value="#1E1E1E"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#5A5A5A"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <Style TargetType="ListBoxItem">
                <Setter Property="Foreground" Value="White"/>
            </Style>
        </Grid.Resources>

        <!-- ====== LAYOUT ====== -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- ====== FARM ====== -->
        <Border Grid.Column="0"
                CornerRadius="10"
                Background="#252526"
                Padding="12"
                Margin="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="FARMA – siatka pól uprawnych"
                           Style="{StaticResource HeaderTextStyle}"/>

                <Border Grid.Row="1"
                        CornerRadius="10"
                        Background="#1E1E1E"
                        Padding="10">
                    <UniformGrid x:Name="FarmGrid" Rows="10" Columns="10"/>
                </Border>
            </Grid>
        </Border>

        <!-- ====== RIGHT PANEL ====== -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- STATS -->
            <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock x:Name="TickText"
                               Text="Tura: 0"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>

                    <TextBlock Text="Statystyki" Style="{StaticResource HeaderTextStyle}"/>

                    <TextBlock Text="Średni wzrost roślin"/>
                    <ProgressBar x:Name="GrowthBar" Minimum="0" Maximum="100" Value="0"/>

                    <TextBlock Text="Średnie nawodnienie" Margin="0,10,0,0"/>
                    <ProgressBar x:Name="WaterBar" Minimum="0" Maximum="100" Value="0"/>
                </StackPanel>
            </Border>

            <!-- ECONOMY -->
            <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Ekonomia" Style="{StaticResource HeaderTextStyle}"/>

                    <TextBlock x:Name="BalanceText"
                               Text="Stan konta: 0 PLN"
                               Foreground="LightGreen"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>

                    <TextBlock Text="Sklep (rośliny)" Margin="0,8,0,0"/>

                    <UniformGrid Rows="1" Columns="3" Margin="0,6,0,0">
                        <Button Content="Kup Pomidor" Click="BuyTomato_Click" Margin="0,0,6,0"/>
                        <Button Content="Kup Róża"    Click="BuyRose_Click"   Margin="0,0,6,0"/>
                        <Button Content="Kup Kaktus"  Click="BuyCactus_Click"/>
                    </UniformGrid>

                    <Button Content="Sprzedaj (z pola)" Click="Sell_Click" Margin="0,10,0,0"/>

                    <Separator Margin="0,10,0,6"/>

                    <Button Content="Nowa gra" Click="NewGame_Click"/>
                </StackPanel>
            </Border>

            <!-- LOGS -->
            <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Logi systemowe" Style="{StaticResource HeaderTextStyle}"/>
                    <ListBox x:Name="LogList" MinHeight="200"/>
                </StackPanel>
            </Border>

        </Grid>
    </Grid>
</Window>
